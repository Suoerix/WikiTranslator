export const DEFAULTS = {
  maxConversionGroups: 5,
  useTslTemplate: true,
  keepOriginalDisplay: true,
  fallbackToTransliteration: true,
  preserveUnknownTerms: true,
  apiRetries: 3,
  apiRetryBaseDelay: 1000,
  batchQueryLimit: 50,
};
